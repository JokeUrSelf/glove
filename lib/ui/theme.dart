import '../dependencies.dart';
/// static class ColorPallet
mixin ColorPallet {
  static Color get name5 =>ColorTheming.tween(light: Colors.white, dark: Colors.black);
  static Color get name2 =>ColorTheming.tween(light: Colors.orange, dark: Colors.green);
}
